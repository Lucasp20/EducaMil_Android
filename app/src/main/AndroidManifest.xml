<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.educamil">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EducaMil">
        <activity
            android:name=".Educamil"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Professor">
            android:theme="@style/Theme.EducaMil.NoActionBar"
            android:label="Professor">
        </activity>

        <activity
            android:name=".Aluno">
            android:theme="@style/Theme.EducaMil.NoActionBar"
            android:label="Aluno">
        </activity>

        <activity android:name=".Disciplina">
            android:label="@string/title_activity_turma"
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>